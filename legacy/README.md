# Legacy experiments

This folder preserves the earlier VICReg prototype, reranking utility, and old
test/trainer scripts for reference. They are not imported by the current
package and may still use the pre-refactor module paths.

Use `python run.py bnneck`, `python run.py transreid`, or
`python run.py proposed` for supported experiments.
